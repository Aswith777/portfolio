# Server Configuration
SERVER_PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://ch-aswith.vercel.app

# Database Configuration (SQL: PostgreSQL / MySQL / H2)
SPRING_PROFILES_ACTIVE=dev
DATABASE_URL=jdbc:postgresql://localhost:5432/aswith_portfolio
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your_secure_password_here

# MongoDB Configuration (Optional for NoSQL profile)
MONGODB_URI=mongodb://localhost:27017/aswith_portfolio

# Email Dispatcher Credentials (Kept strictly private)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=chinthapalliaswith@gmail.com
SMTP_PASSWORD=your_app_specific_password_here
